server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

demo:
  security:
    issuer: "https://issuer.example.local"
    audience: "api://demo"
    introspectionDelayMs: 50
    introspectionCacheTtlMs: 5000
    clockSkewSeconds: 0
